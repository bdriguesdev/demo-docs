name: Deploy docs site

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build and deploy docs site
        run: |
          echo "Building and deploying the docs site..."